import {DefaultWrapper} from '../layouts'

export const meta = {
author: 'Richard'
}

# Oi, eu sou o Test

This is a list in markdown:

- One
- Two
- Three

export default ({ children }) => <DefaultWrapper meta={meta}>{children}</DefaultWrapper>