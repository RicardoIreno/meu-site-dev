import {AboutWrapper} from '../components'

export const meta = {
author: 'Richard'
}

# My MDX Page with a Layout

This is a list in markdown:

- One
- Two
- Three

Checkout my React component:

export default ({ children }) => <AboutWrapper meta={meta}>{children}</AboutWrapper>